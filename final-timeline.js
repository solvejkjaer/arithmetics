function new_timeline() {
  /* defining test timeline*/
  var final = {
    timeline: [{
      type: "instructions",
      pages: [
        'Sus resultados han sido enviados correctamente. Puede cerrar el navegador.'
      ]
  }],
  sample: {type: 'fixed-repetitions', size: 1}
};
return [final];
}
